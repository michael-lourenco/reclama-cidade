/**
 * @deprecated Este serviço foi descontinuado após a migração para NextAuth com PrismaAdapter.
 * A lógica de autenticação e sincronização de usuário agora é tratada automaticamente
 * pelo NextAuth.
 *
 * Para autenticação, use `signIn` e `signOut` de 'next-auth/react'.
 * Para obter dados do usuário, use o hook `useSession` em componentes de cliente
 * ou `getServerSession` em componentes de servidor.
 */
export { };

